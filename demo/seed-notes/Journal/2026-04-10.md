# 2026-04-10

## Morning
- Security audit came back — 6 findings, 2 high (SSRF + path traversal)
- Fixed all 6 in one PR. Added shared path validator + manual redirect walking.
- The SSRF was real — attacker URL could 302 to localhost metadata endpoint

## Afternoon
- Backend team shipped typed SDK wrappers (novyx 3.2.0)
- Swapped raw fetch helpers in lib/control.ts for proper SDK calls
- Added Test Action panel to the governance dashboard — users can now verify policies fire without leaving the UI

## Reflection
- The security audit was the right call. We were live with a real SSRF for weeks.
- Need to bake security checks into the CI pipeline, not rely on manual audits
- The [[Performance Notes|ghost connection cache]] is working well — API calls down 60% since shipping the TTL cache
